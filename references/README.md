# References

This folder is the repo's lightweight reference shelf:

- `hook-patterns.json` for hook selection
- `schema.md` for script-shape and field reference
- `retention.md` for retention heuristics and structure
- `content-audit.md` for lifecycle grouping of `content/`
- `worldcup_elevenlabs_script.md` for the manual TTS worksheet moved out of `content/`
