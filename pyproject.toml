[project]
name = "vidgen"
version = "0.1.0"
description = "TikTok video generation pipeline"
requires-python = ">=3.13.13"
dependencies = [
    "vieneu",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["vidgen"]
